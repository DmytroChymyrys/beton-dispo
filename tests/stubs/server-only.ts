/**
 * Stub for the `server-only` package.
 *
 * The real module throws on import outside a React Server Component, which
 * would make server modules untestable in Node. Aliased in `vitest.config.ts`.
 */
export {};
